import React from 'react';

const SeasonContent = props => {
  console.log(props.lat);
  //SeasonContent Component is being told what the latitude is
  return (
    <div>Season Content</div>
  );
};

export default SeasonContent
